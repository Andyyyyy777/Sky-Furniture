/**
 * OPTIONAL local admin password (this browser only).
 *
 * Copy this file to:  admin-access.local.js
 * Then set a strong password. Never commit admin-access.local.js.
 *
 * Preferred admin access: sign in with a Firebase account listed in
 * ADMIN_EMAILS (config/firebase-keys.js), then open /admin/.
 */
window.SKY_ADMIN_ACCESS = {
  // e.g. "MyStr0ng!Passphrase"
  localPassword: "CHANGE_ME_TO_A_STRONG_PASSWORD"
};
