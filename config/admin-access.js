/**
 * Admin unlock password for this store.
 * Override with admin-access.local.js (gitignored) for a private password.
 */
window.SKY_ADMIN_ACCESS = window.SKY_ADMIN_ACCESS || {
  localPassword: "SkyAdmin2026"
};
